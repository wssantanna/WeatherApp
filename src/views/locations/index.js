import React from 'react';
import { SafeAreaView, Text } from 'react-native';

import Style from './style';

export default LocationScreen = () => {
    return (
        <SafeAreaView style={Style.content}>
            <Text>Lista of the locations</Text>
        </SafeAreaView>
    )
}